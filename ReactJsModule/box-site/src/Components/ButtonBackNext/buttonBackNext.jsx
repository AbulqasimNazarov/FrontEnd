import React from "react";

import './buttonBackNext.scss'

export const ButtonBackNext = ({btnText, icon}) => {
    return (
        <span>
            ok
            <button>ujas</button>
        </span>
    )
}